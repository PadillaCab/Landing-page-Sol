# Example environment variables for Landing-page-Sol
# Backend (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=delsol_db

# Server
PORT=5000
ALLOWED_ORIGIN=http://localhost:3000

# Admin token (proteger, cambiar por un string fuerte en producción)
ADMIN_TOKEN=Mosol2511

# Frontend (Create React App) - opcional para desarrollo
REACT_APP_API_URL=http://localhost:5000
# REACT_APP_ADMIN_TOKEN=your_admin_token_here  # No commits con secretos reales
